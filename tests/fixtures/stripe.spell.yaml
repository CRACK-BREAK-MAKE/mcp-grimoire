# Test fixture for Stripe MCP server
name: stripe
version: 1.0.0
description: Stripe payment processing integration
keywords:
  - payment
  - stripe
  - billing
  - subscription
  - checkout

server:
  transport: http
  url: https://api.stripe.com/mcp
  auth:
    type: bearer
    token: ${STRIPE_API_KEY}

steering: |
  Stripe MCP Server - Payment operations

  Available tools:
  - create_customer: Create new customer
  - create_payment: Process payment
  - list_payments: List recent payments
  - refund_payment: Issue refund

  Best practices:
  - Always handle payment failures gracefully
  - Use idempotency keys for safe retries
  - Test with test mode keys before production
