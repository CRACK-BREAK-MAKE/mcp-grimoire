name: test-http-server
version: 1.0.0
description: Test MCP server via HTTP transport for integration testing
keywords:
  - test
  - http
  - integration
  - sample
server:
  transport: http
  url: http://0.0.0.0:7777/mcp
steering: |
  # Test Server Configuration
  This is a test MCP server using HTTP transport.

  Usage Guidelines:
  - This server is for integration testing only
  - Tools available will depend on what the server exposes
  - HTTP transport uses request/response pattern
